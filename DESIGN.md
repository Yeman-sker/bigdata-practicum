---
version: "2"
name: "Citi Bike 城市运营"
description: "暖白与墨绿的桌面运营工作台，站点信息和地图并排呈现。"
colors:
  canvas: "#F4F3ED"
  surface: "#FFFEFA"
  primary: "#264B3E"
  text: "#22372D"
  muted: "#697368"
  border: "#DDE0D5"
  inventory: "#D18B23"
  shortage: "#B95A3C"
  overflow: "#9B762C"
  replay: "#47796B"
typography:
  family: '-apple-system, BlinkMacSystemFont, "PingFang SC", "Microsoft YaHei", sans-serif'
  body: "14px"
  minimum: "12px"
spacing:
  unit: "8px"
  control-minimum: "44px"
rounded:
  panel: "16px"
  control: "8px"
---

# 城市运营工作台

2026-09-16 按用户要求重设计。本文取代旧版全屏地图、隐藏拨盘、圆形透镜和蓝紫色配色规定。旧设计图已删除，不再作为实现依据。

工作台使用暖白背景、墨绿操作控件、陶土色缺车状态和赭色满桩状态。实色面板、细边线和文字层级承担分区，不使用蓝色大屏、霓虹光效或玻璃浮层。

## 页面结构

顶部呈现产品名称、实时或历史模式、数据来源与状态。实时下直接显示当前库存、未来一小时和调度入口。左侧显示可搜索的站点列表或调度建议，选择站点后在同一区域显示详情。地图位于独立的可伸缩区域，站点详情不覆盖地图。

P0 验收视口为 `1280×720` 及以上桌面。列表和详情可独立滚动。回放控制器保留在地图下方，播放期间不自动隐藏。地图尺寸变化时，标记、路线与点击区域仍须对齐。

## 视觉与信息

- 正文至少 `14px`，次要标签至少 `12px`。数字使用等宽排列，缺失值显示“不可用”或 `—`，不补零。
- 使用系统字体，不引入字体、图标或组件依赖。品牌自行车图形使用内联 SVG。
- 面板使用 `16px` 圆角，控件使用 `8px` 圆角。采用细边线，阴影仅用于需要覆盖其他内容的状态详情。
- 缺车使用陶土色及圆形锚点，满桩使用赭色及方形锚点，同时显示文字状态。轻度风险保留虚线表达。
- 库存粒子仍只表示实际可用车辆，一点对应一辆。预测不改变粒子数量。调度方向、数量与距离用线和文字共同表示。
- 历史流向使用灰绿色，标明骑行次数。进入历史后移除实时库存、风险和调度图层。
- 底图降低饱和度，业务标记保持清楚，地图署名始终可见。
- 列表数量仅描述当前返回的数据，不当作纽约全城指标。开发样例或录制快照来源保持可见。

## 操作与可访问性

主要操作使用原生按钮，点击目标至少 `44×44px`。输入和按钮具备可读名称及可见焦点。状态不能只靠颜色区分。

搜索支持方向键与 Enter。`N` 和 `Shift+N` 巡览站点，`[` 和 `]` 巡览调度建议。Escape 关闭当前详情并恢复焦点，不改变数据模式或日期。减少动态效果时保留静态方向与准确数量。

业务行为见 [产品与交互](docs/product.md)，页面状态见 [前端交互规范](docs/frontend-design.md)，字段与风险规则见 [契约索引](docs/contracts/README.md)。运行中的前端和浏览器验收记录是交付证据。
