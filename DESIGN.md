---
version: prism-1
name: "PRISM / 棱镜空间"
description: "紫银建筑、真实城市轮廓、贴地站点仪表与右侧玻璃操作面板。"
colors:
  background: "#08080d"
  surface: "#17151f"
  surface-translucent: "rgba(20,18,29,0.90)"
  text: "#eeeef4"
  text-muted: "#aaa4b2"
  border: "rgba(233,225,242,0.22)"
  accent: "#e5a9ff"
  focus: "#f4d9ff"
  map-ground: "#15121e"
  water: "#08080f"
  building: "#2d293d"
  inventory: "#f2edf8"
  shortage: "#ffad80"
  overflow: "#f28fca"
  dispatch: "#efa3ff"
  replay: "#c6bcff"
  neutral: "#9893a3"
typography:
  family: "Inter, PingFang SC, Microsoft YaHei, sans-serif"
  label: 12px
  body: 13px
  section: 14px
  title: 16px
  data: 22px
spacing:
  unit: 4px
  gap: 8px
  panel-padding: 16px
  safe-area: 24px
rounded:
  control: 6px
  card: 10px
  panel: 14px
motion:
  ease: "cubic-bezier(0.22, 1, 0.36, 1)"
  fast: 140ms
  base: 240ms
layout:
  desktop-min-width: 1280px
  desktop-min-height: 720px
  instrument-width: 320px
  map-tool-target: 44px
---

# 棱镜空间视觉系统

2026-09-17 用户选定棱镜空间原型作为正式前端方向。本文件取代此前暖白地图、圆形透镜、空间拨盘和后续浅色工作台的视觉规范。旧设计图已移除。数据契约仍为 v1.1。

## 城市与控件

真实城市地图铺满视口。紫银建筑以高倾角展示，深色水面保留海岸轮廓。左上角只显示“Citi Bike · 纽约”、数据来源和时间，不使用无业务含义的品牌代号；左侧竖向工具条集中地图视角、光效开关和装饰密度；右侧窄面板集中模式、搜索、站点详情、风险和调度。历史控制出现在回放模式中。

面板标题直接写当前任务（实时库存、一小时预测、调度建议、历史回放），使用 15px。正文和控件为 11–13px，列表行、分段按钮和输入框保持紧凑高度（约 28–32px），优先保证信息密度；地图工具条命中区仍不小于 44px。站点详情允许一行 3 格紧凑指标（当前车辆、一小时估计、可用空桩），数字最大 22px；除此以外没有大字号宣传语或 KPI 卡片墙。面板、工具条和图例使用同一种纯色半透明底、细边线和柔和投影，不使用渐变填充、发光描边或列表左侧装饰竖条。正文不依赖光晕获得可读性。

右侧面板可纵向滚动，地图保持固定。站点详情与站点列表在同一区域切换。地图保留足够的可操作区域，署名始终可见。1280×720 是桌面最低验收尺寸；更窄窗口不能丢失来源、错误与操作入口。

## 地图和粒子的含义

底图使用 MapLibre 渲染 OpenFreeMap 的真实道路、海岸与建筑，不绘制虚构街区代替加载失败。保留 OpenFreeMap、OpenMapTiles 和 OpenStreetMap 署名。底图与业务接口分别处理加载和失败。

| 图层 | 表达 |
| --- | --- |
| 银白库存粒子 | 一个点对应一辆当前有效库存，围绕站点缓慢公转；数量不随预测视图或装饰密度改变 |
| 贴地风险环与文字 | 按地理半径投影，随倾角和方位变形；由接口的当前或预测状态决定；缺车偏琥珀，满桩偏粉紫并加外环，异常为中性虚线 |
| 环外库存弧 | 弧长 = 当前车辆 / 容量；容量或库存缺失、状态异常时不画 |
| 选中光柱与波纹 | 只标示当前选中的站点或调度端点，不表达数量 |
| 粉紫调度线 | 同批建议的来源到目标；线宽随数量对数变化，流动光点与小箭头指示方向，胶囊标签显示数量与直线距离，不代表已执行路线 |
| 淡紫历史 OD | 所选日期、小时的非零聚合流；方向为起点到终点，不是 GPS 轨迹 |
| 建筑扫描带、道路光点 | 基于真实地图几何的装饰效果：扫描带每 8 秒掠过城市，道路光点带尾迹；不表达库存、风险或骑行数量 |

地图上方叠加一层远景紫雾和四周暗角，只增加纵深感，不表达业务含义。

切换到预测只改变风险表达，库存仍为当前值。进入历史回放移除当前库存、风险、预测和调度。过期、停服、非法和缺失数据不能画成健康状态或零库存。

## 动效与操作

粒子运动、建筑扫描和路线光点使用同一播放开关。装饰密度收在工具条的弹出层中，只调节城市光效，不能改业务数量。

界面动效使用 `motion` 库并统一遵循系统“减少动态效果”：模式和风险筛选的选中块滑动切换，站名从列表行移动到详情标题、详情内容随之淡入，临时提示从地图下方滑入并自动消失（状态条仍负责读屏播报），数字变化时滚动到新值（最终文字始终为精确值），加载中显示占位骨架而不是空白文字。减少动态效果时默认静止，保留方向箭头、状态、准确数量和地图操作；用户仍可手动开启。后台标签停止逐帧绘制。

全城站点（实测 2,520 站、约 3 万个库存点）必须保持流畅：库存粒子由 GPU 绘制，站点圆环、仪表弧和核心光点缓存为静态图层，只在视角或数据变化时重绘；屏幕外站点不绘制。空闲站点菱形在画布上按原尺寸、颜色和叠放顺序绘制，只有悬停附近、选中、调度端点或获得焦点的站点挂载真实按钮，以保留悬停放大、玻璃提示和点击区域（44px 圆形）。键盘通过站点列表和搜索访问全部站点。这些优化不得改变画面：GPU 与画布后备路径使用同一套公式，改动需逐像素对比验证。

按钮、输入和选择器使用原生元素，可见焦点为浅紫色。地图工具命中区至少 44px，紧凑列表保留足够的行高。状态通过颜色、文字和线型共同表达。关闭详情后恢复触发控件焦点。

## 权威边界

本文件定义视觉规范。[前端设计](docs/frontend-design.md)定义页面结构和状态，[产品与交互](docs/product.md)定义五条 P0 行为。业务字段、风险判定与调度算法只由[契约索引](docs/contracts/README.md)指向的文档定义。正式前端不迁入原型中的随机库存或模拟风险算法。
