# 共享单车运营与调度

本项目结合站点库存快照与历史骑行规律，帮助运营人员了解当前供需、判断未来风险并参考调度建议。

## 领域术语

**当前库存状态**：
一个站点依据最新有效库存观测得到的当前供需状态，例如缺车、正常或满桩。它描述当前情况，不因未来预计回流而被改写。
_避免_：用“未来风险”指代当前状态；用一个未区分时间的“风险类型”同时指代当前与预测。

**未来一小时风险**：
依据当前库存与历史基线估计的、一小时后可能出现的站点供需风险。缺少历史基线时，该预测为数据不足，不代表当前库存状态未知或正常。
_避免_：把预测当作当前库存事实；把数据不足称为健康或零风险。

**调度建议**：
为缓解未来一小时的缺车或满桩风险，提出从供车站向补车站搬运车辆的建议。建议必须受当前库存与空桩约束，并避免加剧当前供需问题。
_避免_：把建议称为已执行的调车任务；把地图当前选中的风险视图当作调度依据。

**统一站点标识（canonical station_id）**：
跨历史骑行与当前库存关联同一站点的字符串标识。它与数据提供方自己的 provider station ID 是两个概念。
_避免_：因为两个值都是字符串，就认为它们已经代表同一个站点。

**可服务容量**：
当前可用车辆与可用空桩的合计。它描述当前可服务库存空间，可能不同于站点元数据中的名义容量。

**历史 OD 回放**：
按选定日期和小时展示历史骑行的起终点站聚合流向；不代表当时的连续库存或逐车轨迹。

**库存事件重放**：
再次输入已录制的站点库存快照，用于复现实时处理结果；原观测时刻仍是过去，必须标明录制来源。
