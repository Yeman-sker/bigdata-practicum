# 2026-09-21 联调后修复与交付

Refs #26–#31。基于 `3b362dee6f27119daed06ba391713157197cb0b8`，接续 [首轮真实集成记录](2026-09-21.md)。本次交付真实联调后的本地修复及证据，不宣称 UI 最终验收通过。旧文档 PR #44 的两份证据和 runbook/operations 状态更新已纳入本分支，后续以本次交付 PR 为入口。

## 回放超时与索引

Hermes Computer Use 首轮直接操作真实 API 页面时，2025-01-15 08:00 回放失败；HTTP 约 10 秒后返回 500，后端出现 MySQLTimeoutException。

`dws_station_hourly_flow_v1` 新增 `historical_lookup(dataset_id, service_date, hour)`。新库使用 `sql/serving.sql`，已有库执行 `sql/migrate-historical-lookup.sql`。迁移已在隔离真实全月库执行，并再次执行确认为空操作；没有删除发布数据、延长超时或修改事务隔离。

- availability 查询：迁移前 EXPLAIN 使用 availability 索引并回表过滤，单次 7.614821 秒；迁移后使用 historical_lookup / Using index，单次 0.562394 秒。两次均返回 768 个日期/小时组合，不把未控制缓存的先后观测写成严格性能实验。
- 回放 HTTP：5 次串行、并发度 3 共 9 次请求，均 HTTP 200，每次 7,337 对 OD、8,088 次骑行。逐条测量见 [replay-latency.json](replay-latency.json)。小样本不是生产 SLA。
- 数据仍为 hourly_flow 1,606,848 行、OD 1,987,675 行，OD 总骑行 2,119,681，dataset 与首轮记录一致。
- 增量索引需要 I/O 和 metadata lock；正式部署选择维护窗口，不能因 LOCK=NONE 承诺完全无阻塞。

## 本次保留的前端改进

- 区分拖动预览、请求目标、已成功显示的时间；松开滑块才请求，可用小时之外不提交。
- 取消旧请求，防止旧成功、失败或 availability 返回覆盖新选择；失败保留地图及失败目标，重试失败目标。
- 搜索、风险筛选、每页 50 条站点/建议/OD 明细，保留完整数量；OD 地图筛选不截断明细。
- 刷新不抢地图视角，站点详情返回恢复列表位置及焦点。

用户否定上一轮视觉简化后，已经恢复原始高倾角、逐站标记、风险环、有效库存粒子、默认动效、光效控制及路线效果；历史 OD 默认全部，Top 20 仅手动可选。`DESIGN.md` 与 `map-style.ts` 保持基线原样。本次发布前审查修正了遗留的“聚合/近景选中”图例文案，并增加断言，避免图例与恢复后的渲染不符。未纳入原型设计或新一轮 UI 重构。

## 实际验证与边界

- 本次发布前重跑：前端 12 项全部通过，TypeScript / Vite build 通过，仍有 bundle 大小警告。
- 本次发布前重跑：Python 77 项，74 通过、3 项因无 PySpark 跳过；Ruff 和 diff 检查通过。不能引用首轮 VM 76 项全通过来覆盖本轮跳过项。
- 索引修复阶段：Maven 43 项，0 failures/errors，8 skipped；启用真实 MySQL API 集成，未开启其余 operations/Kafka 集成开关。首轮 43 项零跳过属于另一轮证据。
- Hermes 已在优化阶段两次操作真实页面走通 2025-01-15 08:00，另验证实时搜索站点 2872.02。之后视觉恢复尚未做完整浏览器复验；代码测试不等于观感、交互或五条 P0 全通过。
- 最新提交的 GitHub CI 结果另由交付 PR 记录；本文不预写成功。

## 仍需完成

1. #30 后续 UI 优化与真实全量页面验收：密集标记可读性、五条 P0、OD/预测/建议视觉对账、四档倍速及末小时暂停、键盘焦点、失败恢复、模式隔离和性能。
2. #26–#29 指定消费者的实际签收，以及 #31 尚未记录的生产者/消费者确认；不得以 Agent 自动调用代签。
3. 负责人审查与合并本次交付 PR。本次仅按授权推送与整理任务，不代为合并。

仓库只收纳源码、迁移、测试和脱敏小型证据；原始数据、凭据、临时浏览器资料和原型不入库。
